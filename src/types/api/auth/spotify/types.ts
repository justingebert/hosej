export interface connectSpotifRequest {
    userId: string;
    spotifiyUserId: string;
}
export interface connectSpotifResponse {
    message: string;
}

export interface disconnectSpotifResponse {
    message: string;
}